#!/usr/bin/env zsh

cd "$(dirname $0)"/..

echo ''; hr - '#' -
echo "[BREW FORMULAS]\n"

#brew -v update
brew -v bundle --file=$HOME/.dotjitsu/Brewfile
#brew -v upgrade --file=$HOME/.dotjitsu/Brewfile
#brew unlink git-lfs && brew link git-lfs
#brew cleanup

