#!/bin/bash
set -Eeuo pipefail

cd "$(dirname $0)"/..

hr -

#brew -v update
brew -v bundle --file=$HOME/.dotjitsu/Brewfile
#brew -v upgrade --file=$HOME/.dotjitsu/Brewfile
#brew unlink git-lfs && brew link git-lfs
#brew cleanup

# Force system using Brewfile node.js version (key-only)
brew link --overwrite node@12 --force
