#!/usr/bin/env zsh

dir=${1:-.}
cd $dir

local dirs=`/opt/homebrew/bin/gls -d */ | wc -l`
local files=`/opt/homebrew/bin/gls -p -A | grep -v / | wc -l`
local total=$(( ${dirs} + ${files}))

echo "$dirs dirs, $files files, $total total"
