#!/usr/bin/env zsh

dir=${1:-.}
cd $dir

local dirs=`$HOMEBREW_PREFIX/bin/gls -d */ | wc -l`
local files=`$HOMEBREW_PREFIX/bin/gls -p -A | grep -v / | wc -l`
local total=$(( ${dirs} + ${files}))

echo "$dirs dirs, $files files, $total total"
