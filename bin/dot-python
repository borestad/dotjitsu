#!/usr/bin/env zsh

cd "$(dirname $0)"/..

echo ''; hr -
echo "[Updating Python PIP's]\n"
pip install --upgrade pip

# Glances + deps
pip install --upgrade glances
pip install --upgrade bottle py-cpuinfo pysnmp zeroconf netifaces statsd pystache matplotlib pika

# Colors
pip install --upgrade pygments
