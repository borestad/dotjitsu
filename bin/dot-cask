#!/usr/bin/env zsh

cd "$(dirname $0)"/..

echo "✨ brew bundle (Caskfile)"

if [ -t 1 ] ; then
  # Install Caskfile formulas (only if interactive shell)
  /opt/homebrew/bin/brew cu
  ~/.dotjitsu/bin/hr -
  /opt/homebrew/bin/brew -v bundle --file=~/.dotjitsu/Caskfile
  ~/.dotjitsu/bin/dot-monolingual ~/Applications
fi
