#!/usr/bin/env zsh

cd "$(dirname "$0")"/..

echo ''; hr -
echo "⚡️ Updating npm/pnpm"


pnpm outdated -g

pnpm up --global --latest

# Updates global npm packages
#npm update npm -g
#npm-check -g
#david u -g
