#!/usr/bin/env dash

hr
echo "✨ Adding files\n"
git add . --verbose


hr
echo "✨ Commit files\n"
stats=$(git diff --cached --shortstat)
echo "  $stats"
git commit -m "🔨 BUMP:$stats"

hr
echo "✨ Push files\n"
~/.dotjitsu/bin/push
