#!/usr/bin/env zsh

ROOT=$(git rev-parse --show-toplevel)
cd $ROOT

# if [ ! type "brew" &> /dev/null ]; then
# 	ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
# fi

~/.dotjitsu/bin/git-pull $HOME/.dotjitsu
~/.dotjitsu/bin/git-pull $HOME/.private

~/.dotjitsu/bin/dot-clone

~/.dotjitsu/bin/dot-symlink
~/.private/bin/dotp-symlink

brew install fnm
eval "$(fnm env)"

fnm install --lts
fnm default lts-latest

~/.dotjitsu/bin/dot-brew
~/.private/bin/dot-crontab
~/.dotjitsu/bin/dot-npm-bootstrap

#brew link git-annex
#brew link git-lfs



